docker-compose up -d
docker-compose down
docker-compose ps
docker-compose stop
docker-compose start
docker-compose restart
docker-compose exec -it <containername> /bin/bash
docker-compose exec <containername/ID> ls
docker-compose ps
docker-compose stop
docker-compose start
docker-compose restart 
docker-compose exec -it db /bin/bash
docker-compose exec db ls
docker-compose -f tierwalacompose.yml up -d
